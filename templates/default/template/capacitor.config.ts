import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'com.application.template',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
